# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# macrocast cache
.macrocast/

# Data (downloaded, not committed)
data_cache/
*.csv.gz

# R
macrocastR/.Rproj.user
macrocastR/src/*.o
macrocastR/src/*.so

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.local

# Agent local/runtime state
.claude/
.claire/
.hermes/
.worktrees/
CLAUDE.md

# Internal workflow artifacts (agent comms + design specs)
mailbox.md
implementation.md
plans/design/

# mkdocs build output
site/

# Notebook outputs (examples/results/ is gitignored)
examples/results/

# Local-only research wiki / downloaded source archive (never push)
local_only/
tmp_*.py

# Reboot/archive artifacts (top-level only — plans/archive/ is tracked)
/archive/
REBOOT_IDEA_AND_PLAN_*.md

# Runtime artifacts
*.parquet
runs/
trained_model/

# Sphinx build output
docs/_build/
# docs/reference/ was gitignored when Sphinx auto-generated it; removed in C52
# because docs/reference/ is now a source-committed Diataxis tier directory.
.venv-docs/

# CatBoost training artifacts
catboost_info/

# macrocast runtime output
macrocast_output/

# Local transport artifacts (git format-patch / git bundle output)
*.patch
*.bundle

# graphify artifacts (large; not source)
graphify-input/
graphify-out/

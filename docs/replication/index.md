# Replication

Paper replication guides show how to express published macro-forecasting designs
with the current callable `macroforecast` API. They are not marketing examples.
Each page separates three objects:

1. the paper specification stated in the main text or appendix,
2. the closest reproducible `macroforecast` setting,
3. the remaining gap when the public paper does not expose a vintage, seed,
   software default, or full replication package.

```{toctree}
:maxdepth: 1

macroeconomic_data_transformations_matter
```


# 4.1.5 compute_mode

`compute_mode` controls whether the runtime should run serially or attempt parallel execution over a specific work unit.

This page will be filled in the Layer 0 + Navigator Tree HTML pass.

Current detailed source: [Design Stage 0: compute_mode](../../user_guide/design.md#compute-mode).

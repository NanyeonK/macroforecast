L2.5  --  Pre vs post preprocessing
===================================


:Layer ID: ``l2_5``
:Category: ``diagnostic``
:Sub-layers: L2_5_A_comparison_axis, L2_5_B_distribution_shift, L2_5_C_correlation_shift, L2_5_D_cleaning_effect_summary, L2_5_Z_export

axis ``comparison_pair``  (L2_5_A_comparison_axis)
--------------------------------------------------


:Default: ``'raw_vs_final_clean'``
:Sweepable: False

``multi_stage``
^^^^^^^^^^^^^^^


Compare every L2 stage in sequence (raw → tcoded → outlier → imputed → frame).

L2.5.A comparison stage selector ``multi_stage``. Defines which pair of panel snapshots feeds the comparison output forms below.

**When to use**

Stage-wise attribution of cleaning effects; diagnosing which stage is responsible for unexpected shifts.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``raw_vs_tcoded``, ``raw_vs_outlier_handled``, ``raw_vs_imputed``, ``raw_vs_final_clean``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``raw_vs_final_clean``
^^^^^^^^^^^^^^^^^^^^^^


Compare raw panel vs end-of-L2 panel.

L2.5.A comparison stage selector ``raw_vs_final_clean``. Defines which pair of panel snapshots feeds the comparison output forms below.

**When to use**

Default; one-glance summary of L2's cumulative effect across all four stages.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``raw_vs_tcoded``, ``raw_vs_outlier_handled``, ``raw_vs_imputed``, ``multi_stage``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``raw_vs_imputed``
^^^^^^^^^^^^^^^^^^


Compare raw panel vs L2.D imputed panel.

L2.5.A comparison stage selector ``raw_vs_imputed``. Defines which pair of panel snapshots feeds the comparison output forms below.

**When to use**

Auditing the imputation method's footprint -- detecting bias introduced by EM-factor imputation.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``raw_vs_tcoded``, ``raw_vs_outlier_handled``, ``raw_vs_final_clean``, ``multi_stage``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``raw_vs_outlier_handled``
^^^^^^^^^^^^^^^^^^^^^^^^^^


Compare raw panel vs L2.C outlier-handled panel.

L2.5.A comparison stage selector ``raw_vs_outlier_handled``. Defines which pair of panel snapshots feeds the comparison output forms below.

**When to use**

Auditing winsorisation / replacement effects on tail behaviour.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``raw_vs_tcoded``, ``raw_vs_imputed``, ``raw_vs_final_clean``, ``multi_stage``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``raw_vs_tcoded``
^^^^^^^^^^^^^^^^^


Compare raw panel vs L2.B-transformed panel.

L2.5.A comparison stage selector ``raw_vs_tcoded``. Defines which pair of panel snapshots feeds the comparison output forms below.

**When to use**

Auditing the impact of tcode application -- e.g. confirming log_diff produced stationary series.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``raw_vs_outlier_handled``, ``raw_vs_imputed``, ``raw_vs_final_clean``, ``multi_stage``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``comparison_output_form``  (L2_5_A_comparison_axis)
---------------------------------------------------------


:Default: ``'multi'``
:Sweepable: False

``difference_table``
^^^^^^^^^^^^^^^^^^^^


Table of (after - before) per metric per series.

L2.5.A comparison output form ``difference_table``.

This option configures the ``comparison_output_form`` axis on the ``L2_5_A_comparison_axis`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_A_comparison_axis/`` alongside the other selected views.

**When to use**

Quantifying the magnitude of cleaning shifts; ordered by absolute change for easy ranking.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``side_by_side_summary``, ``overlay_timeseries``, ``distribution_shift``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``distribution_shift``
^^^^^^^^^^^^^^^^^^^^^^


Distribution-overlap statistics (KS / histogram).

L2.5.A comparison output form ``distribution_shift``.

This option configures the ``comparison_output_form`` axis on the ``L2_5_A_comparison_axis`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_A_comparison_axis/`` alongside the other selected views.

**When to use**

Detecting cleaning steps that materially change distributions, not just summary statistics.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``side_by_side_summary``, ``difference_table``, ``overlay_timeseries``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``multi``
^^^^^^^^^


Render every comparison output form together.

L2.5.A comparison output form ``multi``.

This option configures the ``comparison_output_form`` axis on the ``L2_5_A_comparison_axis`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_A_comparison_axis/`` alongside the other selected views.

**When to use**

Comprehensive cleaning audit; recommended default for first-time runs.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``side_by_side_summary``, ``difference_table``, ``overlay_timeseries``, ``distribution_shift``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``overlay_timeseries``
^^^^^^^^^^^^^^^^^^^^^^


Time-series overlay of before / after for each series.

L2.5.A comparison output form ``overlay_timeseries``.

This option configures the ``comparison_output_form`` axis on the ``L2_5_A_comparison_axis`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_A_comparison_axis/`` alongside the other selected views.

**When to use**

Visual confirmation that cleaning preserves dynamics; spotting unexpected shape changes.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``side_by_side_summary``, ``difference_table``, ``distribution_shift``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``side_by_side_summary``
^^^^^^^^^^^^^^^^^^^^^^^^


Side-by-side summary statistics for the chosen pair.

L2.5.A comparison output form ``side_by_side_summary``.

This option configures the ``comparison_output_form`` axis on the ``L2_5_A_comparison_axis`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_A_comparison_axis/`` alongside the other selected views.

**When to use**

Default tabular comparison; matches L1.5.B summary metrics applied to both pre and post.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``difference_table``, ``overlay_timeseries``, ``distribution_shift``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``distribution_metric``  (L2_5_B_distribution_shift)
---------------------------------------------------------


:Default: ``['mean_change', 'sd_change', 'ks_statistic']``
:Sweepable: False

``ks_statistic``
^^^^^^^^^^^^^^^^


Kolmogorov-Smirnov statistic between raw and cleaned distributions.

L2.5.B distribution metric ``ks_statistic`` (multi-select axis).

This option configures the ``distribution_metric`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Detecting that cleaning has moved the distribution non-trivially; KS > 0.1 is a typical 'investigate' threshold.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``mean_change``, ``sd_change``, ``skew_change``, ``kurtosis_change``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``kurtosis_change``
^^^^^^^^^^^^^^^^^^^


Δ-excess-kurtosis before vs after cleaning.

L2.5.B distribution metric ``kurtosis_change`` (multi-select axis).

This option configures the ``distribution_metric`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Detecting heavy-tail removal; large negative changes confirm successful tail trimming.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``ks_statistic``, ``mean_change``, ``sd_change``, ``skew_change``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``mean_change``
^^^^^^^^^^^^^^^


Δ-mean before vs after cleaning.

L2.5.B distribution metric ``mean_change`` (multi-select axis).

This option configures the ``distribution_metric`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Spotting bias introduced by the imputation method (EM-factor can introduce systematic shifts).

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``ks_statistic``, ``sd_change``, ``skew_change``, ``kurtosis_change``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``sd_change``
^^^^^^^^^^^^^


Δ-standard-deviation before vs after cleaning.

L2.5.B distribution metric ``sd_change`` (multi-select axis).

This option configures the ``distribution_metric`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Detecting variance compression from winsorisation / outlier-replacement.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``ks_statistic``, ``mean_change``, ``skew_change``, ``kurtosis_change``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``skew_change``
^^^^^^^^^^^^^^^


Δ-skewness before vs after cleaning.

L2.5.B distribution metric ``skew_change`` (multi-select axis).

This option configures the ``distribution_metric`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Quantifying tail-trimming asymmetry; large skew changes indicate one-sided outlier treatment.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``ks_statistic``, ``mean_change``, ``sd_change``, ``kurtosis_change``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``distribution_view``  (L2_5_B_distribution_shift)
-------------------------------------------------------


:Default: ``'multi'``
:Sweepable: False

``histogram_overlay``
^^^^^^^^^^^^^^^^^^^^^


Overlaid histograms (raw vs cleaned).

L2.5.B distribution view ``histogram_overlay``.

This option configures the ``distribution_view`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Eyeballing distribution-shift magnitude; the most intuitive view for sharing with non-technical stakeholders.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``qq_plot``, ``summary_table``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``multi``
^^^^^^^^^


Produce all three views together.

L2.5.B distribution view ``multi``.

This option configures the ``distribution_view`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Default rich diagnostic for distribution-shift audits.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``histogram_overlay``, ``qq_plot``, ``summary_table``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``qq_plot``
^^^^^^^^^^^


Q-Q plot of cleaned vs raw quantiles.

L2.5.B distribution view ``qq_plot``.

This option configures the ``distribution_view`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Detecting tail-deformation patterns; deviations from the 45° line localise where cleaning altered the distribution.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``histogram_overlay``, ``summary_table``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``summary_table``
^^^^^^^^^^^^^^^^^


Tabular distribution-metric summary.

L2.5.B distribution view ``summary_table``.

This option configures the ``distribution_view`` axis on the ``L2_5_B_distribution_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_B_distribution_shift/`` alongside the other selected views.

**When to use**

Compact numeric summary per series; pairs naturally with ``distribution_metric`` choices.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``histogram_overlay``, ``qq_plot``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``correlation_shift``  (L2_5_C_correlation_shift)
------------------------------------------------------


:Default: ``'none'``
:Sweepable: False

``delta_matrix``
^^^^^^^^^^^^^^^^


Heatmap of (post - pre) correlation matrix.

L2.5.C correlation-shift view ``delta_matrix``.

This option configures the ``correlation_shift`` axis on the ``L2_5_C_correlation_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_C_correlation_shift/`` alongside the other selected views.

**When to use**

Detecting cleaning steps that distort dependence structure; large entries flag pairs whose joint behaviour was altered.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``pre_post_overlay``, ``none``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``none``
^^^^^^^^


Skip correlation-shift diagnostic.

L2.5.C correlation-shift view ``none``.

This option configures the ``correlation_shift`` axis on the ``L2_5_C_correlation_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_C_correlation_shift/`` alongside the other selected views.

**When to use**

When L2 only changes scale (no transform that reshapes correlations).

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``delta_matrix``, ``pre_post_overlay``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``pre_post_overlay``
^^^^^^^^^^^^^^^^^^^^


Side-by-side pre / post correlation heatmaps.

L2.5.C correlation-shift view ``pre_post_overlay``.

This option configures the ``correlation_shift`` axis on the ``L2_5_C_correlation_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_C_correlation_shift/`` alongside the other selected views.

**When to use**

Default visual; reveals magnitude and sign of changes simultaneously.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``delta_matrix``, ``none``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``correlation_method``  (L2_5_C_correlation_shift)
-------------------------------------------------------


:Default: ``'pearson'``
:Sweepable: False

``pearson``
^^^^^^^^^^^


Pearson correlation for the shift comparison.

L2.5.C correlation method ``pearson``.

This option configures the ``correlation_method`` axis on the ``L2_5_C_correlation_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_C_correlation_shift/`` alongside the other selected views.

**When to use**

Default linear-association measure; easiest to interpret.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``spearman``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``spearman``
^^^^^^^^^^^^


Spearman rank correlation.

L2.5.C correlation method ``spearman``.

This option configures the ``correlation_method`` axis on the ``L2_5_C_correlation_shift`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_C_correlation_shift/`` alongside the other selected views.

**When to use**

Robust to outliers; preferred when L2.C may have changed tail behaviour.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``pearson``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``cleaning_summary_view``  (L2_5_D_cleaning_effect_summary)
----------------------------------------------------------------


:Default: ``'multi'``
:Sweepable: False

``multi``
^^^^^^^^^


Render all three counts together.

L2.5.D cleaning effect view ``multi``.

This option configures the ``cleaning_summary_view`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

Default; full cleaning footprint summary.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``n_imputed_per_series``, ``n_outliers_flagged``, ``n_truncated_obs``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``n_imputed_per_series``
^^^^^^^^^^^^^^^^^^^^^^^^


Count of imputed cells per series.

L2.5.D cleaning effect view ``n_imputed_per_series``.

This option configures the ``cleaning_summary_view`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

Auditing imputation footprint; series with > 30% imputed cells warrant inspection.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``n_outliers_flagged``, ``n_truncated_obs``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``n_outliers_flagged``
^^^^^^^^^^^^^^^^^^^^^^


Count of outlier-flagged cells per series.

L2.5.D cleaning effect view ``n_outliers_flagged``.

This option configures the ``cleaning_summary_view`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

Auditing outlier-handler aggressiveness; very high counts may indicate threshold mis-calibration.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``n_imputed_per_series``, ``n_truncated_obs``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``n_truncated_obs``
^^^^^^^^^^^^^^^^^^^


Count of observations dropped by L2.E frame-edge handling.

L2.5.D cleaning effect view ``n_truncated_obs``.

This option configures the ``cleaning_summary_view`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

Auditing edge truncation effects on the available sample size.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``n_imputed_per_series``, ``n_outliers_flagged``, ``multi``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``t_code_application_log``  (L2_5_D_cleaning_effect_summary)
-----------------------------------------------------------------


:Default: ``'summary'``
:Sweepable: False

``none``
^^^^^^^^


Skip the tcode log.

L2.5.D tcode log option ``none``.

This option configures the ``t_code_application_log`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

When ``transform_policy = no_transform`` and no tcodes were applied.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'
* McCracken & Ng (2016) 'FRED-MD: A Monthly Database for Macroeconomic Research', JBES 34(4): 574-589. (doi:10.1080/07350015.2015.1086655)

**Related options**: ``summary``, ``per_series_detail``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``per_series_detail``
^^^^^^^^^^^^^^^^^^^^^


Per-series tcode applied + before/after means.

L2.5.D tcode log option ``per_series_detail``.

This option configures the ``t_code_application_log`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

Forensic audit of tcode application; useful when investigating unexpected post-tcode behaviour.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'
* McCracken & Ng (2016) 'FRED-MD: A Monthly Database for Macroeconomic Research', JBES 34(4): 574-589. (doi:10.1080/07350015.2015.1086655)

**Related options**: ``summary``, ``none``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``summary``
^^^^^^^^^^^


Tcode usage histogram (counts per tcode).

L2.5.D tcode log option ``summary``.

This option configures the ``t_code_application_log`` axis on the ``L2_5_D_cleaning_effect_summary`` sub-layer of L2.5; output is emitted under ``manifest.diagnostics/l2_5/L2_5_D_cleaning_effect_summary/`` alongside the other selected views.

**When to use**

Default; quick cumulative summary of which tcodes were applied.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'
* McCracken & Ng (2016) 'FRED-MD: A Monthly Database for Macroeconomic Research', JBES 34(4): 574-589. (doi:10.1080/07350015.2015.1086655)

**Related options**: ``per_series_detail``, ``none``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``diagnostic_format``  (L2_5_Z_export)
-------------------------------------------


:Default: ``'pdf'``
:Sweepable: False

``csv``
^^^^^^^


L2.5 export -- plain csv table -- one row per series / metric.

Tabular diagnostic outputs (per-series summaries, test p-values, coverage tables) are emitted as comma-separated rows. The fastest path into pandas / R / Excel for ad-hoc analysis.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Quickest path to spreadsheet / pandas; collaborators who avoid JSON.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``html``, ``json``, ``latex_table``, ``multi``, ``pdf``, ``png``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``html``
^^^^^^^^


L2.5 export -- single-file html report with embedded plots and tables.

Renders a self-contained HTML document combining tables (via pandas ``to_html``) and base64-embedded matplotlib figures. Opens in any browser without server-side support; ideal for sharing with stakeholders who lack the codebase.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Sharing diagnostics with collaborators who do not have the repo checked out.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``csv``, ``json``, ``latex_table``, ``multi``, ``pdf``, ``png``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``json``
^^^^^^^^


L2.5 export -- machine-readable json dump of every diagnostic value.

Default format. Round-trips cleanly into Python / JS; mandatory when ``attach_to_manifest=True`` so the diagnostics participate in the manifest's hash-based replication check.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Default; required when diagnostics participate in the bit-exact replication chain.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``csv``, ``html``, ``latex_table``, ``multi``, ``pdf``, ``png``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``latex_table``
^^^^^^^^^^^^^^^


L2.5 export -- latex ``tabular`` snippets ready to ``\input`` into a paper.

Emits one ``.tex`` file per tabular diagnostic, ready to be ``\input`` from a paper draft without further processing. Booktabs-friendly column alignment + automatic column-name escaping.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Paper-quality export when the user is drafting a manuscript.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``csv``, ``html``, ``json``, ``multi``, ``pdf``, ``png``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``multi``
^^^^^^^^^


L2.5 export -- emit json + png + (optional) pdf / html in a single run.

Comprehensive convenience option. Produces JSON for machine consumption + PNG for slides + (when ``latex_export=True``) LaTeX snippets for papers. Equivalent to setting ``diagnostic_format`` separately for each consumer.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Comprehensive runs covering paper, slides and machine-readable consumers in one execution.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``csv``, ``html``, ``json``, ``latex_table``, ``pdf``, ``png``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``pdf``
^^^^^^^


L2.5 export -- vector pdf figures (matplotlib backend).

Matplotlib's PDF backend produces vector graphics that scale without pixelation. Recommended for paper figures where journals require sub-pixel-precise typography.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Publication-grade plots; LaTeX-rendered figures.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``csv``, ``html``, ``json``, ``latex_table``, ``multi``, ``png``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``png``
^^^^^^^


L2.5 export -- rasterised png figures (matplotlib backend).

Matplotlib's AGG backend produces 300dpi-by-default PNG files. Smaller than PDF for plot-heavy reports; the natural choice for slide decks and HTML embeddings.

Diagnostic artifacts land under ``manifest.diagnostics/l2_5/`` with one file per axis per format chosen here.

**When to use**

Slide / web embedding where vector formats are unnecessary.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``csv``, ``html``, ``json``, ``latex_table``, ``multi``, ``pdf``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``attach_to_manifest``  (L2_5_Z_export)
--------------------------------------------


:Default: ``True``
:Sweepable: False

``true``
^^^^^^^^


Embed diagnostic artifacts into manifest.json's diagnostics index.

Default. Diagnostic file paths and content hashes are recorded in ``manifest.diagnostics``, so ``macrocast.replicate(manifest_path)`` validates that every diagnostic re-runs to a bit-identical artifact. Required for reproducibility-critical sweeps.

**When to use**

Default; ensures replication includes the diagnostics.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``false``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``false``
^^^^^^^^^


Keep diagnostic artifacts outside the manifest hash chain.

Files are still written to the run output directory but are not referenced by the manifest, so ``replicate()`` does not validate their hashes. Lighter-weight when the diagnostic surface is large and reproducibility is not the headline concern.

**When to use**

Long-running production sweeps where diagnostics blow up the manifest size.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``true``

.. note:: Last reviewed 2026-05-05 by macrocast author.

axis ``figure_dpi``  (L2_5_Z_export)
------------------------------------


:Default: ``300``
:Sweepable: False

axis ``latex_export``  (L2_5_Z_export)
--------------------------------------


:Default: ``True``
:Sweepable: False

``true``
^^^^^^^^


Co-emit LaTeX table snippets alongside the chosen diagnostic_format.

Independent of ``diagnostic_format`` -- ``latex_export`` is an opt-in extra that always co-emits ``\begin{tabular}`` snippets when the underlying diagnostic is tabular. Saves an extra round-trip through pandas-to-LaTeX when the recipe is feeding a paper draft.

**When to use**

When the recipe is feeding a paper draft.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``false``

.. note:: Last reviewed 2026-05-05 by macrocast author.

``false``
^^^^^^^^^


Skip the LaTeX-table co-emission step.

Default. Avoids the small but non-trivial overhead of pandas-to-LaTeX rendering on every diagnostic axis when no paper-quality output is needed.

**When to use**

Default; LaTeX adds tooling overhead that is wasted for non-paper runs.

**References**

* macrocast design Part 4: 'diagnostic layers default-off; non-blocking; produce JSON + matplotlib views attached to manifest.diagnostics/.'

**Related options**: ``true``

.. note:: Last reviewed 2026-05-05 by macrocast author.

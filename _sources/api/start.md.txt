# `macrocast.start`

Single-run inspection entry point. `macrocast_single_run()` inspects route
ownership, compile status, and tree-context provenance for one recipe path.

```{eval-rst}
.. automodule:: macrocast.start
   :members:
   :show-inheritance:
   :member-order: bysource
```

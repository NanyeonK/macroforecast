# registries

Purpose
- Backing defaults, adapters, contracts, and resolved package-facing registries.
- Distinct from `macrocast/taxonomy/`, which contains the selectable enum universe.
- Distinct from `recipes/`, which will bind taxonomy choices + numeric leaves into named studies.

Current status
- First-pass skeleton layer.
- Mirrors selected transitional config files so migration can proceed incrementally.

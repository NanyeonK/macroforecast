# Contributing

`uv.lock` records the repository development environment for reproducible local
setup and dependency audits; CI still installs from `pyproject.toml` extras and
does not enforce the lockfile.

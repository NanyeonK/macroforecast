recipe_id: custom-benchmark-design
path:
  0_meta:
    fixed_axes:
      research_design: single_path_benchmark
  1_data_task:
    fixed_axes:
      dataset: fred_md
      information_set_type: revised
      target_structure: single_target_point_forecast
    leaf_config:
      target: INDPRO
      horizons: [1, 3]
  2_preprocessing:
    fixed_axes:
      target_transform_policy: raw_level
      x_transform_policy: raw_level
      tcode_policy: raw_only
      target_missing_policy: none
      x_missing_policy: none
      target_outlier_policy: none
      x_outlier_policy: none
      scaling_policy: none
      dimensionality_reduction_policy: none
      feature_selection_policy: none
      preprocess_order: none
      preprocess_fit_scope: not_applicable
      inverse_transform_policy: none
      evaluation_scale: raw_level
  3_training:
    fixed_axes:
      framework: expanding
      benchmark_family: custom_benchmark
      model_family: ar
      feature_builder: autoreg_lagged_target
  4_evaluation:
    fixed_axes:
      primary_metric: msfe
  5_output_provenance:
    leaf_config:
      manifest_mode: full
      benchmark_config:
        minimum_train_size: 5
        plugin_path: path/to/custom_benchmark_plugin.py
        callable_name: custom_benchmark
        notes: "Plugin-ready custom benchmark contract example"
  6_stat_tests:
    fixed_axes:
      stat_test: none
  7_importance:
    fixed_axes:
      importance_method: none

metadata:
  name: l1_with_regime
  description: Layer 1 data definition example with NBER recession regime metadata.

0_meta:
  fixed_axes: {}

1_data:
  fixed_axes:
    dataset: fred_md
    regime_definition: external_nber
  leaf_config:
    target: CPIAUCSL

global_defaults:
  no_lookahead_rule: true
  train_window_only_fit_rule: true
  target_alignment_rule: explicit_required
  benchmark_denominator_lock_rule: explicit_required
  evaluation_scale_explicit_rule: explicit_required
  provenance_mandatory_rule: true
  information_set_policy: revised_data
  vintage_policy: current
  merge_alignment_rule: dataset_default
  release_lag_policy: ignore_release_lags
  ragged_edge_policy: complete_case_only
  variable_universe_version: canonical_current
  sample_period: full_sample
  estimation_start: dataset_default
  oos_period: config_defined
  minimum_train_size: 120
  benchmark_family: ar
  benchmark_id: ar_bic_expanding
  forecast_type: direct
  horizon_target_construction: point_at_t_plus_h
  overlapping_horizon_treatment: hac_in_evaluation
  evaluation_scale: explicit_required
  outer_window: expanding
  refit_policy: every_oos_step
  validation_design: last_block
  cv_scheme: time_split
  same_split_across_models: true
  same_split_across_targets: true
  horizon_specific_split: false
  shuffled_split_forbidden: true
  target_x_preprocessing_independence: true
  preprocessing_operation_order: target_then_x
  target_preprocess_recipe: basic_none
  x_preprocess_recipe: basic_none
